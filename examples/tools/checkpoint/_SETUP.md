# Checkpoint setup

Install this directory as a `checkpoint` domain under the repository's `_workflow/`
directory. Keep `create` executable.

State in the repository's root agent guidance that checkpoints create local commits and are
pushed only on explicit request or for an existing upstream pull request. Configure each
supported coding agent to narrowly approve the installed command.
